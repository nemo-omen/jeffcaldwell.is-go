package about

import "jeffcaldwell.is/view/layout"

templ Index(current, remoteAddr string) {
	@layout.Base("JeffCaldwell — About", current, remoteAddr) {
		<section class="content-container page">
			<div class="section-header highlight">
				<h2>About</h2>
			</div>
		</section>
	}
}
